#include<stdio.h>
int main()
{
	int i,k,n;
	char x[100],y[100];
	scanf("%s",&x);
	scanf("%s",&y);
	for(i=0;a[i]!='\0';i++)
	{
		if(a[i]==b[i])
		{
			printf("%c",a[i]);
		}
		else
		{
			break;
		}
	}
	return 0;
}
